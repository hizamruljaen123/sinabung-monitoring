// ─── Theme Logic (no-op, theme now handled in index.html inline) ───────────
// updateChartColors kept for backward compat
function updateChartColors(isDark) { /* charts use fixed dark palette */ }
